import branding from '../../branding.json' with { type: 'json' };

export const {
  appBundleId,
  appName,
  cliCommand,
  cliScriptName,
  configDirName,
  configFileName,
  urlScheme,
} = branding;
